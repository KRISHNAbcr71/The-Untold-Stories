<%-include("../../views/partials/admin/header")%>
  <link rel="stylesheet" href="/css/sales.css">


  <div class="container-fluid p-4">

    <!-- PAGE TITLE -->
    <h2 class="mb-4 fw-bold">📊 Sales Report</h2>

    <!-- ================= FILTER SECTION ================= -->
    <div class="card mb-4">
  <div class="card-body">

    <!-- QUICK FILTERS + RESET -->
    <div class="row align-items-center mb-3">
      <div class="col-md-8">
        <label class="form-label fw-semibold">Quick Filters</label>
        <div>
          <button class="btn btn-outline-primary btn-sm quick-filter" data-filter="today">Today</button>
          <button class="btn btn-outline-primary btn-sm quick-filter" data-filter="week">This Week</button>
          <button class="btn btn-outline-primary btn-sm quick-filter" data-filter="month">This Month</button>
          <button class="btn btn-outline-primary btn-sm quick-filter" data-filter="year">This Year</button>
          <button class="btn btn-outline-secondary btn-sm" id="customBtn">Custom</button>
        </div>
      </div>

      <div class="col-md-4 text-end">
        <button class="btn btn-outline-danger btn-sm" id="resetFilters">
          Reset Filters
        </button>
      </div>
    </div>

    <!-- CUSTOM DATE RANGE (HIDDEN INITIALLY) -->
    <div id="customRange" class="row g-3 align-items-end d-none">
      <div class="col-md-4">
        <label class="form-label fw-semibold">From</label>
        <input type="date" id="fromDate" class="form-control">
      </div>

      <div class="col-md-4">
        <label class="form-label fw-semibold">To</label>
        <input type="date" id="toDate" class="form-control">
      </div>

      <div class="col-md-4">
        <button class="btn btn-primary px-4" id="generateReport">
          Generate Report
        </button>
      </div>
    </div>

  </div>
</div>

    

    <!-- ================= GRAPH SECTION ================= -->
    <div class="row mb-4">
      <div class="col-md-8">
        <div class="card">
          <div class="card-body">
            <h6 class="fw-bold mb-3">📈 Sales Trend</h6>
            <canvas id="salesChart" height="177"></canvas>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card">
          <div class="card-body">
            <h6 class="fw-bold mb-3">🍩 Discount Breakdown</h6>

            <canvas id="discountChart" class="mb-3"></canvas>

            <!-- Discount Values -->
            <div class="discount-info">
              <div class="d-flex justify-content-between">
                <span>Offer Discount</span>
                <strong>₹<%= summary.offerDiscount %></strong>
              </div>
              <div class="d-flex justify-content-between">
                <span>Coupon Discount</span>
                <strong>₹<%= summary.couponDiscount %></strong>
              </div>
              <hr>
              <div class="d-flex justify-content-between">
                <span>Total Discount</span>
                <strong>₹<%= summary.offerDiscount + summary.couponDiscount %></strong>
              </div>
            </div>

          </div>
        </div>
      </div>

    </div>

    <!-- ================= SUMMARY CARDS ================= -->
    <div class="row mb-4">

      <div class="col-md-4">
        <div class="summary-card">
          <p>Total Orders</p>
          <h4>
            <%= summary.totalOrders %>
          </h4>
        </div>
      </div>

      <div class="col-md-4">
        <div class="summary-card">
          <p>Gross Sales</p>
          <h4>₹<%= summary.grossSales %>
          </h4>
        </div>
      </div>

      <div class="col-md-4">
        <div class="summary-card">
          <p>Net Revenue</p>
          <h4>₹<%= summary.netRevenue %>
          </h4>
        </div>
      </div>

      <div class="col-md-4">
        <div class="summary-card">
          <p>Offer Discount</p>
          <h4>₹<%= summary.offerDiscount %>
          </h4>
        </div>
      </div>

      <div class="col-md-4">
        <div class="summary-card">
          <p>Coupon Discount</p>
          <h4>₹<%= summary.couponDiscount %>
          </h4>
        </div>
      </div>

      <div class="col-md-4">
        <div class="summary-card">
          <p>Total Discount</p>
          <h4>₹<%= summary.offerDiscount + summary.couponDiscount %>
          </h4>
        </div>
      </div>

    </div>

    <!-- ================= SALES REPORT TABLE ================= -->
    <div class="card mb-4">
      <div class="card-body">
        <h6 class="fw-bold mb-3">📋 Sales Details</h6>

        <div class="table-responsive">
          <table class="table table-bordered align-middle">
            <thead class="table-light">
              <% if (orders.length===0) { %>
                <tr>
                  <td colspan="7" class="text-center text-muted">No orders found</td>
                </tr>
                <% } %>

                  <tr>
                    <th>Order ID</th>
                    <th>Date</th>
                    <th>Customer</th>
                    <th>Gross</th>
                    <th>Offer Discount</th>
                    <th>Coupon Discount</th>
                    <th>Net Amount</th>
                  </tr>
            </thead>
            <tbody>
              <% orders.forEach(order=> {%>
                <tr>
                  <td>
                    <%= order.orderId %>
                  </td>
                  <td>
                    <%= order.createdAt.toLocaleDateString() %>
                  </td>
                  <td>
                    <%= order.user ? order.user.name : 'Deleted User' %>
                  </td>
                  <td>₹<%= order.grossAmount %>
                  </td>
                  <td>₹<%= order.offerDiscount %>
                  </td>
                  <td>₹<%= order.discountAmount %>
                  </td>
                  <td class="fw-bold">₹<%= order.finalAmount %>
                  </td>
                </tr>
                <%})%>
            </tbody>
          </table>

          <!-- ================= PAGINATION ================= -->
          <ul class="pagination pagination-custom justify-content-end mt-3">

            <li class="page-item <%= pagination.currentPage === 1 ? 'disabled' : '' %>">
              <a class="page-link" href="?page=<%= pagination.currentPage - 1 %>">Previous</a>
            </li>

            <% for (let i=1; i <=pagination.totalPages; i++) { %>
              <li class="page-item <%= pagination.currentPage === i ? 'active' : '' %>">
                <a class="page-link" href="?page=<%= i %>&filter=<%= filter %>&from=<%= from %>&to=<%= to %>">
                  <%= i %>
                </a>
              </li>
              <% } %>

                <li class="page-item <%= pagination.currentPage === pagination.totalPages ? 'disabled' : '' %>">
                  <a class="page-link" href="?page=<%= pagination.currentPage + 1 %>&filter=<%= filter %>&from=<%= from %>&to=<%= to %>">Next</a>
                </li>

          </ul>



        </div>


      </div>
    </div>

    <!-- ================= DOWNLOAD SECTION ================= -->
    <div class="text-end">
      <a href="/admin/downloadSalesReportPDF?filter=<%= filter %>&from=<%= from %>&to=<%= to %>" class="btn btn-outline-danger me-2">
        Download PDF
      </a> 


      <a href="/admin/downloadSalesReportExcel?filter=<%= filter %>&from=<%= from %>&to=<%= to %>" class="btn btn-success">
        Download Excel
      </a>
    </div>

  </div>



  <script id="salesTrendData" type="application/json"><%- JSON.stringify(salesTrend || []) %></script>
  <script id="discountData" type="application/json">
  <%- JSON.stringify({ 
    offerDiscount: summary?.offerDiscount || 0, 
    couponDiscount: summary?.couponDiscount || 0 
    }) %>
  </script>
  <script>
  document.addEventListener('DOMContentLoaded', () => {
    const urlParams = new URLSearchParams(window.location.search);
    const currentFilter = urlParams.get('filter') ;
    
    document.querySelectorAll('.quick-filter').forEach(btn => {
      if (btn.dataset.filter === currentFilter) {
        btn.classList.add('active');
      }
    });

    /* ===== QUICK FILTER BUTTONS ===== */
    document.querySelectorAll('.quick-filter').forEach(btn => {
      btn.addEventListener('click', () => {
        const filter = btn.dataset.filter;
        const url = new URL(window.location);
        url.searchParams.set('filter', filter);
        
        if (filter !== 'custom') {
          url.searchParams.delete('from');
          url.searchParams.delete('to');
        }
        
        window.location.href = url.toString();
      });
    });

    /* ===== GENERATE REPORT BUTTON ===== */
    document.getElementById('generateReport')?.addEventListener('click', () => {
      const from = document.getElementById('fromDate').value;
      const to = document.getElementById('toDate').value;

      if (!from || !to) {
        Swal.fire('Error', 'Please select both From and To dates', 'error');
        return;
      }

      window.location.href = `/admin/sales?filter=custom&from=${from}&to=${to}`;
    });

    const salesTrendData = JSON.parse(document.getElementById('salesTrendData').textContent || '[]');
    const discountData = JSON.parse(document.getElementById('discountData').textContent || '{}');

    if (salesTrendData.length > 0) {
      const labels = salesTrendData.map(d => d.label);
      const revenue = salesTrendData.map(d => d.totalRevenue);
      
      let chartType = 'bar';
      if (labels.length > 20) chartType = 'line';
      
      new Chart(document.getElementById('salesChart'), {
        type: chartType,
        data: {
          labels,
          datasets: [{
            data: revenue,
            backgroundColor: '#d4a373',
          }]
        },
        options: {
          responsive: true,
          plugins: {
            legend: { display: false }
          },
          scales: {
            y: {
              beginAtZero: true,
              ticks: {
                callback: function(value) {
                  return '₹' + value;
                }
              }
            }
          }
        }
      });
    } else {
      document.getElementById('salesChart').innerHTML = 
        '<div class="text-center text-muted p-4">No data available for the selected period</div>';
    }

    /* ========== DISCOUNT CHART ========== */
    if (discountData.offerDiscount > 0 || discountData.couponDiscount > 0) {
      new Chart(document.getElementById('discountChart'), {
        type: 'pie',
        data: {
          labels: ['Offer Discount', 'Coupon Discount'],
          datasets: [{
            data: [
              discountData.offerDiscount || 0,
              discountData.couponDiscount || 0
            ],
            backgroundColor: [
              '#d4a373',
            '#faedcd',
            '#e9edc9',
            '#ccd5ae',
            '#b7b7a4'
            ]
          }]
        },
        options: { 
          responsive: true,
          plugins: {
            legend: {
              position: 'bottom'
            }
          }
        }
      });
    }

    // Custom date range visibility
    const customBtn = document.getElementById('customBtn');
    const customRange = document.getElementById('customRange');
    const resetBtn = document.getElementById('resetFilters');

    if (customBtn && customRange) {
      customBtn.addEventListener('click', () => {
        customRange.classList.remove('d-none');
        
        // Set today as default dates
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('fromDate').value = today;
        document.getElementById('toDate').value = today;
        
        // Deactivate quick filters
        document.querySelectorAll('.quick-filter').forEach(btn => {
          btn.classList.remove('active');
        });
      });
    }

    if (resetBtn) {
      resetBtn.addEventListener('click', () => {
        window.location.href = '/admin/sales';
      });
    }

    if (currentFilter === 'custom') {
      customRange?.classList.remove('d-none');
      if (urlParams.get('from')) {
        document.getElementById('fromDate').value = urlParams.get('from');
      }
      if (urlParams.get('to')) {
        document.getElementById('toDate').value = urlParams.get('to');
      }
    }
  });

  
</script>

  <%-include("../../views/partials/admin/footer")%>