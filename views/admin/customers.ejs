<%-include("../../views/partials/admin/header")%>
  <link rel="stylesheet" href="/css/customers.css">



  <!-- Heading -->
  <!-- <h2 class="mb-4">Customers</h2> -->

  <!-- Search Bar -->
  <!-- <div class="search-container">
    <input type="text" class="form-control" id="searchInput" placeholder="Search customers...">
    <span class="search-clear" id="clearSearch" style="display:none;">&times;</span>
  </div> -->
  <!-- Heading and Search in One Line -->

  <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-3">
    <h2 class="mb-0">Customers</h2>

    <div class="search-container position-relative">
      <input type="text" class="form-control" id="searchInput" placeholder="Search customers..."
        value="<%= search || '' %>">
      <span class="search-clear" id="clearSearch" style="display:none;">&times;</span>
    </div>


   

  </div>


   <!-- If no results -->
<% if (noResults) { %>
  <div class="text-center text-danger mb-3">No results found</div>
<% } %>


  <!-- Customer Table -->
  <%if(data){%>
    <div class="table-responsive">
      <table class="table table-bordered table-striped text-center align-middle mx-auto" style="width: 80%;">
        <thead class="table-secondary">
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody id="customerTableBody">
          <%for(let i=0; i<data.length; i++){%>
            <tr>
              <td>
                <%=data[i].name%>
              </td>
              <td>
                <%=data[i].email%>
              </td>
              <td>
                <%=data[i].phone%>
              </td>
              <td>
                <%if(data[i].isBlocked===false){%>

                  <a class="btn btn-sm btn-danger" href="/admin/blockCustomer?id=<%=data[i]._id%>"
                    style="text-decoration: none; color: white;"
                    onclick="return confirmAction(event,'block','<%= data[i]._id %>')">Block</a>

                  <%}else{%>

                    <a class="btn btn-sm btn-success" href="/admin/unblockCustomer?id=<%=data[i]._id%>"
                      style="text-decoration: none; color: white;"
                      onclick="return confirmAction(event,'unblock','<%= data[i]._id %>')">Unblock</a>

                    <%}%>
              </td>
            </tr>
            <%}%>
              <!-- Add more rows dynamically -->
        </tbody>
      </table>
    </div>
    <%}else{%>
      <div class="alert alert-warning text-center mt-4">
        🔍 No User found
      </div>
      <%}%>






        <!-- <div class="container mt-3">
          <nav aria-label="Page Navigation">
            <ul class="pagination justify-content-center mb-20" style="margin-right: 200px;">
              <% if (typeof totalPages !=='undefined' && totalPages> 1) { %>
                <% for(let i=1; i <=totalPages; i++) { %>
                  <li class="page-item <%=(i === currentPage) ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>">
                      <%= i %>
                    </a>
                  </li>
                  <% } %>
                    <% } else { %>
                      <li class="page-item disabled">
                        <span class="page-link">No Pages Available</span>
                      </li>
                      <% } %>
            </ul>
          </nav>
        </div> -->

        <!-- <div class="d-flex justify-content-center mt-4">
          <nav>
            <ul class="pagination">
              <%for(let i=1; i<=totalPages; i++){%>
                <li class="page-item <%=(i===currentPage)?'custom-active-page':'custom-normal-page'%>">
                  <a class="page-link" href="?page=<%= i%>">
                    <%= i%>
                  </a>
                </li>
                <%}%>
            </ul>
          </nav>
        </div> -->

        <div class="container mt-3">
          <nav aria-label="Page Navigation">
            <ul class="pagination justify-content-center mb-20" style="margin-right: 200px;">

                <% for(let i=1; i <=totalPages; i++) { %>
                  <li class="page-item <%=(i === currentPage) ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>">
                      <%= i %>
                    </a>
                  </li>
                  <% } %>
                    
            </ul>
          </nav>
        </div>







        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

        <script>
          const searchInput = document.getElementById('searchInput');
          const clearBtn = document.getElementById('clearSearch');

          // Show/hide clear button based on input
          searchInput.addEventListener('input', () => {
            clearBtn.style.display = searchInput.value ? 'block' : 'none';
          });

          // Trigger search on Enter
          searchInput.addEventListener('keypress', function (e) {
            if (e.key === 'Enter') {
              const query = searchInput.value.trim();
              window.location.href = `/admin/users?search=${encodeURIComponent(query)}&page=1`;
            }
          });

          // Clear search
          clearBtn.addEventListener('click', () => {
            searchInput.value = '';
            clearBtn.style.display = 'none';
            window.location.href = '/admin/users'; // Reload full list
          });

          // Initialize clear button on page load
          if (searchInput.value) {
            clearBtn.style.display = 'block';
          }






          function confirmAction(event, action, id) {
            event.preventDefault(); // Prevent the default <a> click

            const actionText = action === 'block' ? 'Block' : 'Unblock';
            const buttonColor = action === 'block' ? '#d33' : '#3085d6';

            Swal.fire({
              title: 'Are you sure?',
              text: `You are about to ${actionText.toLowerCase()} this user.`,
              icon: 'warning',
              showCancelButton: true,
              confirmButtonColor: buttonColor,
              cancelButtonColor: '#aaa',
              confirmButtonText: `Yes, ${actionText} it!`
            }).then((result) => {
              if (result.isConfirmed) {
                fetch(`/admin/${action}Customer?id=${id}`, {
                  method: 'GET'
                })
                  .then(response => {
                    if (!response.ok) {
                      throw new Error('Failed to ' + action);
                    }
                    return response.json();
                  })
                  .then(data => {
                    Swal.fire(
                      `${actionText}ed!`,
                      `User has been ${actionText.toLowerCase()}ed successfully.`,
                      'success'
                    ).then(() => {
                      location.reload(); // Refresh the page to show updated status
                    });
                  })
                  .catch(error => {
                    Swal.fire(
                      'Error!',
                      'Something went wrong.',
                      'error'
                    );
                  });
              }
            });

            return false; // Prevent default behavior
          }






        </script>


        <%-include("../../views/partials/admin/footer")%>